export default class {
  constructor(name, date, weekday, first, last, total) {
    this.name = name;
    this.date = date;
    this.weekday = weekday;
    this.first = first;
    this.last = last;
    this.total = total;
  }
}
